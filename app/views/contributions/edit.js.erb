$("#show-contribution-<%= @contribution.id %> .primary .content").append("<%= j render(partial: "contributions/form", locals: { contribution: @contribution }) %>");
$("#edit-contribution-content-<%= @contribution.id %>").focus();
