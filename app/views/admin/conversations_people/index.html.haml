%h2 Conversations Moderators
%table.conversations_people.tablesorter(summary="List of Moderators")
  %thead
    %tr
      %th ID
      %th Conversation
      %th Moderator
      %th(colspan="3") Actions
  %tbody
    - @moderators.each do |moderator|
      %tr{"data-moderator-id" => moderator.id}
        %td= moderator.id
        %td= link_to short_title(moderator.conversation.title), conversation_url(moderator.conversation)
        %td= link_to moderator.person.name, admin_person_path(moderator.person)
        %td= link_to "Show", admin_conversations_person_path(moderator.id)
        %td= link_to "Edit", edit_admin_conversations_person_path(moderator.id)
        %td= link_to "Delete", admin_conversations_person_url(moderator.id), :method => :delete, :confirm => "Are you sure?"

%br
= link_to 'Add Moderator', new_admin_conversations_person_path
