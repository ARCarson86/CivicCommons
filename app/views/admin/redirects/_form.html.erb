<%= form_for [:admin, redirect] do |f| %>
  Redirect from <%= "#{request.protocol}#{request.host}" %>/<%= f.text_field :path, placeholder: "path" %> to <%= "#{request.protocol}#{request.host}" %>/<%= f.text_field :destination, placeholder: "destination path" %>
  <%= f.submit 'Create' %>
<% end %>
