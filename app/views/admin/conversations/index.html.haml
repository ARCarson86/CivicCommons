%h2 Conversations
%table.conversations.tablesorter(summary="List of Conversations")
  %thead
    %tr
      %th.handle_header
      %th ID
      %th Conversation Title
      %th Created Date
      %th.staff_pick_header Staff Pick
      %th Moderator
      %th.actions_header(colspan="2") Actions
  %tbody
    - @conversations.each do |conversation|
      %tr{ "data-conversation-id" => conversation.id, :class => conversation.staff_pick ? '' : 'ui-state-disabled' }
        %td.handle &#11021;
        %td= conversation.id
        %td= link_to short_title(conversation.title), admin_conversation_path(conversation)
        %td= conversation.created_at.strftime("%m-%d-%Y")
        %td.staff_pick_column= toggle_staff_pick_link(conversation)
        %td
          - if conversation.moderators.count > 0
            = conversation.moderators.collect(&:name).join(', ')
            = link_to "Manage Moderators", admin_conversation_conversations_people_path(conversation)
          - else
            = link_to "Add Moderators", admin_conversation_conversations_people_path(conversation)
        %td= link_to "Edit", edit_admin_conversation_path(conversation)
        %td= link_to "Delete", admin_conversation_url(conversation), :method => :delete, :confirm => "Are you sure?"
%br

