<div class="author-card mini">
  <%= author_card(author) %>
  <div class="name">
    <%= text_profile(author) %>
  </div>
  <div class="date">
    on <%= conversation_created_at.strftime('%B %d, %Y') %>
  </div>
</div>

