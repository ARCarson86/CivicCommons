<script type='text/javascript' >
  $.colorbox({
    html: "<%= escape_javascript(
            raw(
              render( :partial => 'sessions/new' )
            )
    ) %>",
    onComplete: function(){
      $.colorbox.resize();
    }
  });

  
</script>