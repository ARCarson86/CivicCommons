<li class="with-borders nav-user-info pull-right">
    <%= user_profile(current_person).html_safe %>
    <%= text_profile(current_person).html_safe %>
    <a id="profile-more" href="#">
        <i class="icon-caret-down"></i>
    </a>
  <div id="profile-options" class="hide">
    <div><%= link_to_settings(current_person).html_safe %> </div>
    <div><%= link_to "Logout", destroy_person_session_path, :class => 'user-link' %></div>
  </div>
</li>
